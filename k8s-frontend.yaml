# Frontend Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nest-frontend-deployment
  labels:
    app: nest-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nest-frontend
  template:
    metadata:
      labels:
        app: nest-frontend
    spec:
      containers:
      - name: nest-frontend-container
        image: nest-frontend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000

---
# Frontend Service
apiVersion: v1
kind: Service
metadata:
  name: nest-frontend-service
  labels:
    app: nest-frontend
spec:
  type: NodePort
  selector:
    app: nest-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30303